name: Add to Project

on:
  issues:
    types:
      - opened
      - reopened
      - transferred
  pull_request_target:
    types:
      - opened
      - reopened
      - ready_for_review

permissions:
  contents: read

jobs:
  add-to-project:
    uses: JorisJonkers-dev/github-workflows/.github/workflows/add-to-project.yml@248d3e6d729e262cf098eb045e9e579cb1e52011 # main
    with:
      project-url: https://github.com/orgs/JorisJonkers-dev/projects/2
    secrets:
      PROJECT_AUTOMATION_APP_ID: ${{ secrets.RELEASE_APP_ID }}
      PROJECT_AUTOMATION_APP_PRIVATE_KEY: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
